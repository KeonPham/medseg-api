"""Register trained models in MLflow model registry."""

import logging

logger = logging.getLogger(__name__)


def main() -> None:
    """Register a model checkpoint with MLflow and record metrics."""
    raise NotImplementedError("Model registration not yet implemented")


if __name__ == "__main__":
    logging.basicConfig(level=logging.INFO)
    logger.info("Model registration script — not yet implemented")
