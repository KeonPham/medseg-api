"""Export PyTorch models to ONNX format for optimized inference."""

import logging

logger = logging.getLogger(__name__)


def main() -> None:
    """Export a trained model checkpoint to ONNX format."""
    raise NotImplementedError("ONNX export not yet implemented")


if __name__ == "__main__":
    logging.basicConfig(level=logging.INFO)
    logger.info("ONNX export script — not yet implemented")
