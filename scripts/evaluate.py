"""Evaluate trained models on test datasets."""

import logging

logger = logging.getLogger(__name__)


def main() -> None:
    """Run evaluation and report metrics for all model variants."""
    raise NotImplementedError("Evaluation script not yet implemented")


if __name__ == "__main__":
    logging.basicConfig(level=logging.INFO)
    logger.info("Evaluation script — not yet implemented")
